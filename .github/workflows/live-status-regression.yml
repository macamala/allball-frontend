name: Screenshot live status regression gate
on:
  push:
    branches: [fix/live-status-screenshots-20260925]
    paths: ['audit/apply_live_status.py', '.github/workflows/live-status-regression.yml']
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: python audit/apply_live_status.py
      - run: npm ci
      - run: |
          mkdir -p /tmp/live-status-evidence
          npm test -- --reporter=default --reporter=junit --outputFile=/tmp/live-status-evidence/tests.xml
          npm run build
      - if: always()
        run: |
          mkdir -p /tmp/live-status-evidence
          git add -N src
          git diff --binary > /tmp/live-status-evidence/source.patch
          sha256sum /tmp/live-status-evidence/source.patch
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: live-status-regression-evidence
          path: /tmp/live-status-evidence
          retention-days: 30
  publish-tested-source:
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 2
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
      - run: |
          python audit/apply_live_status.py
          git add src
          git config user.name 'NinkoSports regression checks'
          git config user.email '79318740+macamala@users.noreply.github.com'
          git commit -m 'Respect final status, reject stale deltas and prevent sparse alias resurrection'
          git push origin HEAD:fix/live-status-screenshots-20260925
          echo "TESTED_SOURCE_COMMIT=$(git rev-parse HEAD)"
